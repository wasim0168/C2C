
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>OLX Clone</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
  <link href="/css/style.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
</head>
<style>
  body {
   overflow-x:hidden;
  }
  .navbar-nav .nav-link {
    transition: all 0.3s ease;
    border-radius: 8px;
  }

  .navbar-nav .nav-link:hover {
    background-color: #EF990F;
    color: #2C2C2C !important;
  }

  .btn-search {
    background-color: #EF990F;
    color: #2C2C2C;
    border: none;
  }

  .btn-search:hover {
    background-color: #d5810e;
  }

  .form-control {
    border-radius: 8px;
  }

  .navbar-brand span {
    color: #EF990F;
    font-weight: 700;
  }

  .nav-link.active {
    background-color: #EF990F;
    color: #2C2C2C !important;
  } 
</style>
<body>
 <nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #254c63;">
    <div class="container">
      <a class="navbar-brand" href="/">
        <!-- <span class="fs-3">Sell4Anything</span> -->
        <img src="/uploads/Sell4anything (2).png" alt="Logo" class="ms-2" style="width: 200px; height: 60px;">
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item mx-1">
            <a class="nav-link active fw-semibold" href="/"><i class="bi bi-house-door-fill me-1"></i>Home</a>
          </li>

          <% if (user && !user.is_admin) { %>
          <li class="nav-item mx-1">
            <a class="nav-link fw-semibold" href="/profile"><i class="bi bi-person-square me-1"></i>My Profile</a>
          </li>
          <li class="nav-item mx-1">
            <a class="nav-link fw-semibold" href="/products/create"><i class="bi bi-bag-check-fill me-1"></i>Sell Item</a>
          </li>
          <% } %>

          <% if (user && user.is_admin) { %>
          <li class="nav-item mx-1">
            <a class="nav-link fw-semibold" href="/admin/dashboard"><i class="bi bi-speedometer2 me-1"></i>Admin Dashboard</a>
          </li>
          <% } %>
        </ul>

        <form class="d-flex mb-2 mb-lg-0 me-lg-3" action="/search" method="GET">
          <input class="form-control me-2" type="search" name="q" placeholder="Search products..." aria-label="Search">
          <button class="btn btn-search" type="submit"><i class="bi bi-search"></i></button>
        </form>

        <ul class="navbar-nav mb-2 mb-lg-0">
          <% if (user) { %>
          <li class="nav-item mx-1">
            <a class="nav-link fw-semibold" href="/auth/logout"><i class="bi bi-box-arrow-right me-1"></i>Logout</a>
          </li>
          <% } else { %>
          <li class="nav-item mx-1">
            <a class="nav-link fw-semibold" href="/auth/login"><i class="bi bi-box-arrow-in-right me-1"></i>Login</a>
          </li>
          <li class="nav-item mx-1">
            <a class="nav-link fw-semibold" href="/auth/register"><i class="bi bi-person-plus-fill me-1"></i>Register</a>
          </li>
          <li class="nav-item mx-1">
            <a class="nav-link fw-semibold" href="#"><i class="bi bi-question-circle-fill me-1"></i>Help</a>
          </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>
<div class="container mt-3">
  <% if (typeof message !== 'undefined' && message) { %>
    <div class="alert alert-info"><%= message %></div>
  <% } %>
</div>